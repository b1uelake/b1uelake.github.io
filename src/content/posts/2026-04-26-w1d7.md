---
title: '构建GPT-计算图可视化'
published: 2026-04-26
description: '定义函数的意义-->为了调用，让代码更简单'
tags: ['AI', '机器学习']
category: 'GPT-2'
draft: false
---

## python知识点


定义函数的意义-->为了调用，让代码更简单
#### 定义函数 def ()的（）中是否有内容的区别：
没内容-->不需要传参数就能调用
有内容-->需要传参数就能调用
![w1d7-1777438998024.webp](/images/w1d7-1777438998024.webp)

#### 定义函数中有return和没return的区别
有return--有返回值，这个值会存起来，可以查看，可以再使用
没return--没返回值，这个值不会存起来，不能查看，无法再使用

例：
无return，运行不会有任何结果，也无法存储信息-->默认返回None，不可再使用
![w1d7-1777463987171.webp](/images/w1d7-1777463987171.webp)

有return，运行有结果，能存储信息-->可再使用
![w1d7-1777464128660.webp](/images/w1d7-1777464128660.webp)


#### 函数中变量的作用域

就拿上面这张图举例子，我们在函数中给f赋值为20，但是只要在函数之外，这个f就没有任何定义。
![w1d7-1777464538172.webp](/images/w1d7-1777464538172.webp)
运行报错，f没定义。
此时，要让赋值的f在函数内外都有定义，要怎么办呢？
1.使用global. 
![w1d7-1777464723812.webp](/images/w1d7-1777464723812.webp)

2.直接查看return的返回值
![w1d7-1777464859165.webp](/images/w1d7-1777464859165.webp)

3.使用类属性，将变量存在一个对象里

![w1d7-1777466544550.webp](/images/w1d7-1777466544550.webp)

## micograd--续


``` python
# 画一个图便于看计算过程
from graphviz import Digraph

def trace(root):
  # builds a set of all nodes and edges in a graph
  nodes, edges = set(), set()
  def build(v):
    if v not in nodes:
      nodes.add(v)
      for child in v._prev:
        edges.add((child, v))
        build(child)
  build(root)
  return nodes, edges

def draw_dot(root):
  dot = Digraph(format='svg', graph_attr={'rankdir': 'LR'}) # LR = left to right
  
  nodes, edges = trace(root)
  for n in nodes:
    uid = str(id(n))
    # for any value in the graph, create a rectangular ('record') node for it
    dot.node(name = uid, label = "{ %s | data %.4f | grad %.4f }" % (n.label, n.data, n.grad), shape='record')
    if n._op:
      # if this value is a result of some operation, create an op node for it
      dot.node(name = uid + n._op, label = n._op)
      # and connect this node to it
      dot.edge(uid + n._op, uid)

  for n1, n2 in edges:
    # connect n1 to the op node of n2
    dot.edge(str(id(n1)), str(id(n2)) + n2._op)

  return dot
  
draw_dot(L)
```
![w1d7-1777462679556.webp](/images/w1d7-1777462679556.webp)

这幅图表示了连续的的计算过程图中的data代表输入的数据，grad表示梯度。

下面让它更还原神经网络训练的过程

``` python
# inputs x1,x2
x1 = Value(2.0, label='x1')
x2 = Value(0.0, label='x2')
# weights w1,w2
w1 = Value(-3.0, label='w1')
w2 = Value(1.0, label='w2')
# bias of the neuron
b = Value(6.8813735870195432, label='b')
# x1*w1 + x2*w2 + b
x1w1 = x1*w1; x1w1.label = 'x1*w1'
x2w2 = x2*w2; x2w2.label = 'x2*w2'
x1w1x2w2 = x1w1 + x2w2; x1w1x2w2.label = 'x1*w1 + x2*w2'
n = x1w1x2w2 + b; n.label = 'n'
o = n.tanh(); o.label = 'o'

draw_dot(o)
```
![w1d7-1777462917810.webp](/images/w1d7-1777462917810.webp)
x1 x2表示输入的数据， w1 w2表示输入的权重（参数）
进行数学计算之后得到损失值，我们的目的是为了让损失值减小。于是我们之前的_backward函数就起到了反向计算的作用，从右往左计算出最初输入的权重w1 w2的梯度。
- **梯度 > 0**：代表 $w$ 和 $Loss$ 是**正相关**的。$w$ 增大，$Loss$ 也会增大。
    
- **梯度 < 0**：代表 $w$ 和 $Loss$ 是**负相关**的。$w$ 增大，$Loss$ 反而会减小。

所以减小梯度>0的权重，或者增加梯度<0的权重，让损失减小。

